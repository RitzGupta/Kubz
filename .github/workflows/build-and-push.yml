name: Build and push to ECR

# Manual dispatch only - porter-lite triggers this from the UI. Nothing builds
# on push, so a commit is built exactly once, when you ask for it.
on:
  workflow_dispatch:
    inputs:
      app:
        description: "App to build (blank = every app in application.yml)"
        required: false
        default: ""
      environment:
        description: "Environment the image is built for"
        required: false
        default: dev
        type: choice
        options: [dev, qa, stage, prod]

env:
  AWS_REGION: ap-south-1
  AWS_ROLE_ARN: arn:aws:iam::147956724891:role/github-actions-kubz-ecr-push

permissions:
  contents: read
  id-token: write

jobs:
  # The app list comes from application.yml so the workflow cannot drift from
  # the spec porter-lite deploys.
  plan:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.apps.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4

      - name: Read apps from application.yml
        id: apps
        run: |
          python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import json, os, yaml

          spec = yaml.safe_load(open("application.yml"))
          only = os.environ.get("ONLY", "").strip()

          apps = [
              {
                  "name": a["name"],
                  "context": a.get("build", {}).get("context", "."),
                  "dockerfile": a.get("build", {}).get("dockerfile", "Dockerfile"),
              }
              for a in spec["apps"]
              if not only or a["name"] == only
          ]
          if not only and not apps:
              raise SystemExit("application.yml declares no apps")
          if only and not apps:
              raise SystemExit(f"no app named {only!r} in application.yml")

          print("matrix=" + json.dumps({"include": apps}))
          PY
        env:
          ONLY: ${{ inputs.app }}

  build:
    needs: plan
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false          # one broken app must not hide the others
      matrix: ${{ fromJson(needs.plan.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v4

      - name: Build ${{ matrix.name }} (${{ inputs.environment }})
        run: |
          docker build \
            -t "${{ matrix.name }}:${{ github.sha }}" \
            -f "${{ matrix.context }}/${{ matrix.dockerfile }}" \
            "${{ matrix.context }}"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # One ECR repository per app AND environment: "<app>-<environment>".
      # porter-lite creates it before dispatching this build. Tags are
      # immutable, so a commit can only be pushed once - treat an image that is
      # already there as success, since re-running a build is normal.
      - name: Tag and push ${{ matrix.name }}
        run: |
          REGISTRY="${{ steps.login-ecr.outputs.registry }}"
          REPO="${{ matrix.name }}-${{ inputs.environment }}"
          SHA="${{ github.sha }}"
          IMAGE="$REGISTRY/$REPO"
          LOCAL="${{ matrix.name }}:$SHA"   # what the build step tagged

          # Create the repository if it is missing, so a build never fails with
          # "repository does not exist". Immutable tags: a commit is pushed once.
          if ! aws ecr describe-repositories --repository-names "$REPO" \
                 --region "$AWS_REGION" >/dev/null 2>&1; then
            echo "creating ECR repository $REPO"
            aws ecr create-repository \
              --repository-name "$REPO" \
              --image-tag-mutability IMMUTABLE \
              --region "$AWS_REGION" >/dev/null
          fi

          # batch-get-image, not describe-images: the CI role is not granted
          # ecr:DescribeImages. It exits 0 either way, so compare the output.
          FOUND=$(aws ecr batch-get-image \
                    --repository-name "$REPO" \
                    --image-ids imageTag="$SHA" \
                    --region "$AWS_REGION" \
                    --query 'images[0].imageId.imageTag' \
                    --output text 2>/dev/null || true)

          if [ "$FOUND" = "$SHA" ]; then
            echo "$IMAGE:$SHA is already in ECR - nothing to push"
            exit 0
          fi

          docker tag "$LOCAL" "$IMAGE:$SHA"
          docker push "$IMAGE:$SHA"
          echo "pushed $IMAGE:$SHA"
